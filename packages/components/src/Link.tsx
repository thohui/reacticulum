import type { LinkProps } from '@reacticulum/types';
export type { LinkProps };

export function Link({ children, to, align, backgroundColor, bold, color, italic, underline }: LinkProps) {
	return null;
}
