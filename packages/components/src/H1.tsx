import { StyleProps } from "./types";

export interface H1props extends StyleProps {
  children: React.ReactNode;
}

export function H1({
  children,
  align,
  backgroundColor,
  bold,
  color,
  italic,
  underline,
}: H1props) {
  return null;
}
